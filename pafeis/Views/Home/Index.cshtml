
@{
    ViewBag.Title = "Home";
    Layout = "~/Views/Shared/_Home.cshtml";
}
<style>

    #loginform {
        margin-top: -10px;
        padding-right: 25px;
    }

        #loginform label {
            color: #fff;
        }
	.login-panel h1, .login-panel h2 { 
    text-align: center;}
		.login-form {
    background-color: #eeeeee;
    border: 1pt solid #3071a8;
    min-height: 127px;
    padding-top: 19px;
}

.container {
    width: 939px !important;
    margin-top: 250px;
}

</style>

<div class="pageHeader">
        <div class="container-fluid">
            <div class="row">
                <div class="col-xs-2 pull-left">
                    <a href="@Url.Content("~/")" class="navbar-brand">
                        <img style="float: left; position: relative; z-index: 100;width: 173px;margin-left: 532px;margin-top: 104px;" 
                        src="@Url.Content("~/")images/loginAirforce.png" style="float: left;  position: relative; z-index: 100; " >
                    </a>
                </div>
                
            </div>
        </div>
</div>
<div class="container page">
	<div class="login-panel col-md-12">
		<h1>PAFEIS  </h1>
		<h2></h2>
		<form id="frmLogin" method="post" action="@Url.Content("~/")account/validate" class="login-form col-sm-6 col-sm-offset-3 col-md-4 col-md-offset-4 col-lg-4 col-lg-offset-4">  
			<div class="form-horizontal">
				<div class="form-group  ">  <label class=" col-xs-4 control-label">Login Name</label>
					<div class=" col-xs-7"><input type="text" name="username" id="username" class="form-control input-sm "></div>
				</div>
				<div class="form-group  ">  <label class=" col-xs-4 control-label">Password</label>
					<div class=" col-xs-7"><input type="password" name="password" id="password" class="form-control input-sm "></div>
				</div> 
				<div class="buttonGroup col-xs-offset-4">
					<button type="submit" class="btn btn-primary btn-sm" id="btnLogIn">Log In</button>    
				</div>            
			</div>        
		</form>        
	</div>			
</div>

<script>
    $(document).ready(function () {
	    var msg = zsi.getUrlParamValue("access");
		var _username = readCookie("username");
		if(_username){
			if (msg !== "invalid") {
				$("#username").val(_username);
				$("#password").val( readCookie("userpwd"));
				$("#isremember").prop('checked',true);
				$("#frmLogin").submit();
			}
			else
				clearCookies();
		}
        if (msg === "invalid") {
            alert("Invalid Access. Please try again.")
        }
		
        $('.carousel').carousel({
            interval: 6000
        })		
    });

$("#frmLogin").submit(function () {

    if ($("#username").val() === "") { alert("Please enter username."); return false; }
    if ($("#password").val() === "") { alert("Please enter password."); return false; }
	checkRememberSignin();
});

$("#btnTest").click(function () {
	checkRememberSignin();
});

function clearCookies(){
	createCookie("username","",0);
	createCookie("userpwd","",0);
	$("#isremember").prop('checked',false);
}

function checkRememberSignin(){
	if ( $("#isremember").is(':checked') ){
			createCookie("username",$("#username").val(),1);
			createCookie("userpwd",$("#password").val(),1);
	}
	else clearCookies();
}

var button = $("#example-one");
button.on("click", function () {

    var x = $(this);
    if (x.text() == x.data("text-swap")) {
        x.text(x.data("text-original"));
    } else {
        x.data("text-original", x.text());
        x.text(x.data("text-swap"));
    }

});

var button = $("#example-two");
button.on("click", function () {

    var x = $(this);
    if (x.text() == x.data("text-swap")) {
        x.text(x.data("text-original"));
    } else {
        x.data("text-original", x.text());
        x.text(x.data("text-swap"));
    }

});
function createCookie(name,value,days) {
    var expires;
    if (days) {
    	var date = new Date();
    	date.setTime(date.getTime()+(days*24*60*60*1000));
    	expires = "; expires="+date.toGMTString();
    }
    else expires = "";
    document.cookie = name+"="+value+expires+"; path=/";
}
</script>




