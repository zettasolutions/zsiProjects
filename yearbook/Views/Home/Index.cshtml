@{
    ViewBag.Title = "Home";
    Layout = "~/Views/Shared/_Home.cshtml";
}
<style>
body,
html {
	margin: 0;
	padding: 0;
	height: 100%;
	background: #343a40 !important;
}
.user_card {
	height: 318px;
	width: 350px;
	margin-top: auto;
	margin-bottom: auto;
	position: relative;
	display: flex;
	justify-content: center;
	flex-direction: column;
	padding: 10px;
	box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2), 0 6px 20px 0 rgba(0, 0, 0, 0.19);
	-webkit-box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2), 0 6px 20px 0 rgba(0, 0, 0, 0.19);
	-moz-box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2), 0 6px 20px 0 rgba(0, 0, 0, 0.19);
	border-radius: 5px;
}
.brand_logo_container {
	position: absolute;
	height: 170px;
	width: 170px;
	top: -82px;
	left:90px;
	border-radius: 73% 51%;
	padding: 9px;
	text-align: center;
}
.brand_logo {
	height: 150px;
	width: 150px;
	border-radius: 50%;
	border: 2px solid white;
}
.form_container {
	margin-top: 0px;
}
.login_btn {
	width: 100%;
	background: #F11716 !important;
	color: white !important;
}
.login_btn:focus {
	box-shadow: none !important;
	outline: 0px !important;
}
.login_container {
	padding: 0 2rem;
}
.input-group-text {
	background: #F11716 !important;
	color: white !important;
	border: 0 !important;
	border-radius: 0.25rem 0 0 0.25rem !important;
}
.input_user,
.input_pass:focus {
	box-shadow: none !important;
	outline: 0px !important;
}
.custom-checkbox .custom-control-input:checked~.custom-control-label::before {
	background-color: #F11716 !important;
}
.t-title {
    font-weight: bolder !important;
    color: #f8f8f8 !important;
    margin-bottom: 2rem;
    margin-top: 4rem;
    font-size: 2rem;
}
.lear-logo {
	color: #F11716;
	font-size: 9.5rem;
}

#video-viewport {
    position: absolute;
    z-index: 1;
    width: 100%;
}
#video-haftone {
    position: absolute;
    z-index: 2;
    width: 100%;
    opacity: 0.6;
    height: 100%;
    background-image: url(/images/video_texture.png);
    background-repeat: repeat;
    background-color: #33323a;
}

.login-panel {
    position: absolute;
    width: 100%;
    z-index: 3;
    height: 100%;
    display: flex;
}

.login-skin {
	height: 318px;
    width: 350px;
    margin: auto auto;
    z-index: 3;
	opacity: 0.3;
	overflow: hidden;
}

#video-viewport video{
	width: 100%;
}

.login-viewport {
    position: absolute;
    z-index: 3;
    width: 100%;
    height: 100%;
	display:flex;
}

svg.mysvg {
    height: 533px;
    width: 585px;
}

.st0{
	fill:#BDBCBC;
}
   
</style>
<div id="video-viewport">
            <video preload="auto" loop autoplay muted >
                <source src="/images/videos/lear_clips.mp4" type="video/mp4">
                <source src="/images/videos/lear_clips.webm" type="video/webm">
            </video>
</div>
<div id="video-haftone"></div>
<div class="login-panel">
	<div class="login-skin">
		<svg class="mysvg" viewBox="0 0 350 318" >
		<path class="st0" d="M204.9,0.3h-51.5c-1.8,12-8.1,24.2-18.6,33.7c-21.3,19.3-51.8,20.4-68,2.5c-8.8-9.7-11.6-23-9-36.3H4.9
			C2.2,0.3,0,2.5,0,5.1v181.1c0,2.7,2.2,4.9,4.9,4.9h200c2.7,0,4.9-2.2,4.9-4.9V5.1C209.7,2.5,207.6,0.3,204.9,0.3z"></path>

		</svg>
    </div>
	
</div>
<div class="login-viewport">
		<div class="container h-100">
			<div class="d-flex justify-content-center h-100">
				<div class="user_card">
					<div class="d-flex justify-content-center">
						<div class="brand_logo_container">
							<i class="ttool lear-logo" alt="Logo"></i> 
						</div>
					</div>
					<div class="mt-3">
						<div class="d-flex justify-content-center">
							<h1 class="t-title">TREND TOOL</h1>
						</div>
						
					</div>
					<div class="d-flex justify-content-center form_container">
						<form id="frmLogin" method="post" action="@Url.Content("~/")account/validate" class="">
							
							<div class="input-group mb-3">
								<div class="input-group-append">
									<span class="input-group-text"><i class="fas fa-user"></i></span>
								</div>
								<input type="text" name="username" class="form-control input_user" value="" placeholder="username">
							</div>
							<div class="input-group mb-2">
								<div class="input-group-append">
									<span class="input-group-text"><i class="fas fa-key"></i></span>
								</div>
								<input type="password" name="password" class="form-control input_pass" value="" placeholder="password">
							</div>
							<div class="d-flex justify-content-center mt-3 login_container">
								<button type="submit" class="btn btn-primary id="btnLogIn">Log In</button>
							</div>
						</form>
					</div>
				</div>
			</div>
		</div>
	</div>
<script>
    $(document).ready(function () {
	    var msg = zsi.getUrlParamValue("access");
	    var _username = readCookie("username");

		if(_username){
			if (msg !== "") {
				$("#username").val(_username);
				$("#password").val( readCookie("userpwd"));
				$("#isremember").prop('checked',true);
				$("#frmLogin").submit();
			}
			else
				clearCookies();
		}
		else {
		    if (msg) alert( unescape(msg) + " Please try again.")
        }

        $('.carousel').carousel({
            interval: 6000
        })
    });

$("#frmLogin").submit(function () {

    if ($("#username").val() === "") { alert("Please enter username."); return false; }
    if ($("#password").val() === "") { alert("Please enter password."); return false; }
	checkRememberSignin();
});

$("#btnTest").click(function () {
	checkRememberSignin();
});

function clearCookies(){
	createCookie("username","",0);
	createCookie("userpwd","",0);
	$("#isremember").prop('checked',false);
}

function checkRememberSignin(){
	if ( $("#isremember").is(':checked') ){
			createCookie("username",$("#username").val(),1);
			createCookie("userpwd",$("#password").val(),1);
	}
	else clearCookies();
}

var button = $("#example-one");
button.on("click", function () {

    var x = $(this);
    if (x.text() == x.data("text-swap")) {
        x.text(x.data("text-original"));
    } else {
        x.data("text-original", x.text());
        x.text(x.data("text-swap"));
    }

});

var button = $("#example-two");
button.on("click", function () {

    var x = $(this);
    if (x.text() == x.data("text-swap")) {
        x.text(x.data("text-original"));
    } else {
        x.data("text-original", x.text());
        x.text(x.data("text-swap"));
    }

});

function createCookie(name,value,days) {
    var expires;
    if (days) {
    	var date = new Date();
    	date.setTime(date.getTime()+(days*24*60*60*1000));
    	expires = "; expires="+date.toGMTString();
    }
    else expires = "";
    document.cookie = name+"="+value+expires+"; path=/";
}

function readCookie(name) {
    var nameEQ = name + "=";
    var ca = document.cookie.split(';');
    for (var i = 0; i < ca.length; i++) {
        var c = ca[i];
        while (c.charAt(0) == ' ') c = c.substring(1, c.length);
        if (c.indexOf(nameEQ) === 0) return c.substring(nameEQ.length, c.length);
    }
    return null;
}
</script>
